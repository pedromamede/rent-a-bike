<%= form_with(model: ponto) do |form| %>
  <% if ponto.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ponto.errors.count, "error") %> prohibited this ponto from being saved:</h2>

      <ul>
        <% ponto.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :image_url, style: "display: block" %>
    <%= form.text_field :image_url %>
  </div>

  <div>
    <%= form.label :endereco, style: "display: block" %>
    <%= form.text_area :endereco %>
  </div>

  <div>
    <%= form.label :horario, style: "display: block" %>
    <%= form.text_field :horario %>
  </div>

  <div>
    <%= form.label :link_maps, style: "display: block" %>
    <%= form.text_field :link_maps %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
